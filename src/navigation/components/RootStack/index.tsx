import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {RootScreenParams} from '../../interfaces';

export const RootStack = createNativeStackNavigator<RootScreenParams>();
